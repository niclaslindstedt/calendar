<?xml version="1.0" encoding="utf-8"?>
<!--
  SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
  The provider's manifest metadata: what the widget picker shows, and how the
  launcher may size it.

  `updatePeriodMillis` is 0 on purpose. The system's own period is capped at
  30 minutes and would wake the provider for nothing most of the time; the
  content changes when the app publishes a snapshot (the bridge module sends
  an explicit APPWIDGET_UPDATE) and when the date rolls over — and a date
  change already broadcasts ACTION_DATE_CHANGED, which the receiver listens
  for in the app manifest (see plugins/with-widgets.js).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/calendar_widget"
    android:description="@string/calendar_widget_description"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0" />
