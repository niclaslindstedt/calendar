---
type: Fixed
title: Top menu spacing wherever the page runs under the status bar
---

The top menu now takes the larger of its own padding and the status-bar inset, rather than stacking the one on the other everywhere but the installed iOS PWA. The App Store build, which runs the page edge to edge, gets the same even gap above and below the buttons as the installed app.
