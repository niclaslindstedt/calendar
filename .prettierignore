dist/
node_modules/
coverage/
package-lock.json
# Generated by the release workflow from the conventional-commit stream — its
# released sections are machine-written, so they aren't held to Prettier's
# style (and must never be hand-edited to satisfy it).
CHANGELOG.md
# Verbatim copy of the governing spec.
OSS_SPEC.md
# The native wrapper's generated trees: `expo prebuild` output (regenerated
# from native/app.config.js and its plugins) and the packed web build.
native/ios/
native/android/
native/.expo/
native/assets/
